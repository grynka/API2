{
  "name": "api2",
  "version": "2.0.0",
  "description": "MySQL TecDoc 2016 Express API",
  "scripts": {
    "start": "cross-env NODE_ENV=production node ./server.js",
    "start:dev": "cross-env NODE_ENV=development nodemon ./server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@sendgrid/mail": "^7.7.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mariadb": "^3.1.1",
    "morgan": "^1.10.0",
    "nanoid": "^3.0.0",
    "nodemon": "^2.0.21"
  },
  "devDependencies": {
    "cross-env": "^7.0.3"
  }
}
